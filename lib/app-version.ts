// Kept in sync with package.json's "version" by hand at each release —
// simplest way to avoid bundler-specific JSON-import quirks for a value
// that only changes a couple of times per phase.
export const APP_VERSION = "3.3.0";
