Project: LUXSTICKER AI
Version: 3.3.0
Build: build001
Date: 2026-08-14 12:42:20 +0700
Previous Version In Repo: 3.1.0 (see Known Limitations — the spec's stated "Current: 3.2.0" did not match the actual codebase; no 3.2.0 was ever built)
AI Mode: mock (default; set AI_MODE=real + AI_PROVIDER=openai + AI_PROVIDER_API_KEY to enable real generation)
AI Provider: openai (gpt-image-1, via /v1/images/edits) — unchanged interface from Phase 3; see docs/ai-provider.md section 10 for the Phase 3.3 Model/Provider evaluation against spec's 5 criteria (image-to-image, character reference, character consistency, pose control, style transformation)
AI Model: gpt-image-1
Phase 3.3 root-cause fix: EMOTION_EXPRESSION_MAP gave every occurrence of a repeated emotion the exact same {expression, pose} pair. Replaced with EMOTION_EXPRESSION_POOL + resolveExpressionForOccurrence() (config/expression-presets.ts), cycling per-emotion the same way composition presets already did — the concrete fix for "same character, same pose" packs.
Catalog expansion: PoseId +7 (hand_on_cheek, sleeping, running, shy_pose, surprised_pose, cover_mouth, looking_sideways), ExpressionId +4 (confident, playful, relaxed, embarrassed), new IntentId concept (10 values, config/intent-catalog.ts) driving an optional Action clause in the AI prompt.
Prompt engine: PROMPT_VERSION v3 -> v4. Restructured into labeled sections (Identity/Art Style/Expression+Action+Pose/Camera Framing/Composition/Negative), explicitly permits hand/arm/body/head position to move as part of performing a pose, never includes Thai text (verified: no Thai characters in any catalog .description field).
Quality gate: new AIArtworkScore (types/index.ts) — real heuristic scores for imageQuality/singleSubject/identityConsistency, honestly null (with stated reason) for poseAdherence/expressionAdherence/artifactFree (no ML/OCR/pose-estimation model available offline). Multi-subject and text-contamination proxies now hard-reject and feed a bounded 3-attempt retry loop (prompt refinement -> pose refinement -> fallback; never unlimited calls).
Mock provider fix: MockExpressionProvider no longer bakes a "MOCK - NO AI" badge into the actual image pixels (previously drawn via canvas onto the exported PNG) - now a pure passthrough of the original cutout. Mock status is signaled only via UI badges (unchanged) and a required window.confirm + BUILD_INFO "AI Source:" line before a mock-built pack can be exported (lib/pack-export.ts).
Real-photo AI failure messaging: AiFailureBanner.tsx now shows the exact required string "Provider นี้ไม่รองรับการเปลี่ยนท่าทางที่เชื่อถือได้" when style="real" and AI pose/expression generation fails.
Tests: 16/16 Phase 3.3 spec-driven tests independently executed and passing in this sandbox (26 assertions, /tmp/verify-phase-3.3.mjs) + Phase 3.1's 20/20 tests re-verified as a regression check — see TESTING.md
Lint: PASS (npx eslint . — 0 errors, 0 warnings)
TypeScript: PASS (npx tsc --noEmit — 0 errors)
Build: PASS (npm run build — Next.js 16.3.0 / Turbopack, clean production build)
Known Limitations:
  - Version discrepancy: the Phase 3.3 request stated "Current: 3.2.0" but the actual codebase (verified via package.json and lib/app-version.ts before any Phase 3.3 work started) was 3.1.0 — no 3.2.0 was ever built in this project's history. This build proceeds pragmatically as 3.1.0 -> 3.3.0 per the explicitly requested target version; surfaced here rather than silently assumed.
  - This sandbox's outbound network is still restricted to registry.npmjs.org (unchanged since Phase 3, re-confirmed for Phase 3.3). No live AI call could be made or observed from this environment — no claim of a passing "Real AI Test" is made anywhere in this build's report.
  - No Reference Images were actually attached to the Phase 3.3 request in this session (checked and confirmed empty) — everything was built from the detailed text specification alone.
  - poseAdherence / expressionAdherence / artifactFree in AIArtworkScore are always null (honestly, not approximated) — these require pose-estimation/facial-landmark ML unavailable offline in this sandbox.
  - Multi-subject and text-contamination detection are coarse geometric/texture heuristics (connected-region count, local edge-variance tiling), not a real person-detector or OCR — documented with their exact limits in lib/canvas-utils.ts.
  - Retry attempt 3 uses a pose-refinement prompt variant, not a provider/model swap — this app's AIImageProvider registry only ever resolves one configured provider at a time; no second pre-configured fallback vendor exists to swap to automatically.
  - Full details: TESTING.md (Phase 3.3 section) and docs/ai-provider.md (sections 10-13).
